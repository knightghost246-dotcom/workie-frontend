import React from 'react'; import { Link, useLocation } from 'react-router-dom';
export default function BottomNav({role}){const loc=useLocation();const p=role==='customer'?'/dashboard-customer':'/dashboard-worker';const items=[{icon:'🏠',label:'Home',path:p},{icon:'📋',label:'Orders',path:'/order-status'},{icon:'💬',label:'Chat',path:'/inbox'},{icon:'💳',label:'Wallet',path:'/wallet'},{icon:'👤',label:'Profile',path:'/worker-profile'}];return(<div style={{position:'fixed',bottom:0,left:'50%',transform:'translateX(-50%)',maxWidth:402,width:'100%',background:'#fff',display:'flex',justifyContent:'space-around',padding:'10px 0 16px',borderTop:'1px solid #E2E8F0',zIndex:100}}>{items.map((x,i)=><Link key={i} to={x.path} style={{display:'flex',flexDirection:'column',alignItems:'center',gap:2,fontSize:20,color:loc.pathname===x.path?'#0D9488':'#94A3B8',textDecoration:'none'}}><span>{x.icon}</span><span style={{fontSize:10}}>{x.label}</span></Link>)}</div>)}
